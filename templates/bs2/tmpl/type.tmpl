<?js
	var data = obj;
    var self = this;
    data.forEach(function(name, i) { ?>
&nbsp;&nbsp;<span class="param-type label label-info"><?js= self.linkto(name, self.htmlsafe(name)) ?></span>
        <?js if (i < data.length - 1) { ?>|<?js } ?>
    <?js }); ?>
